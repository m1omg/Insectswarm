{
  "name": "insectswarm",
  "version": "0.1.0",
  "private": true,
  "description": "A scientifically grounded simulation of four social insect colonies, running in the browser with no build step.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "serve": "python3 -m http.server 8000"
  }
}
